name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - index.html
      - study.html
      - mock.html
      - 404.html
      - assets/**
      - q/**
      - c/**
      - robots.txt
      - sitemap.xml
      - site.webmanifest
      - googled42fe79d25dbdc9a.html
      - scripts/validate-data.mjs
      - scripts/validate-site.mjs
      - .github/workflows/deploy.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Validate browser scripts
        run: |
          node --check assets/data.js
          node --check assets/app.js
          node scripts/validate-data.mjs
          node scripts/validate-site.mjs

      - name: Prepare site artifact
        run: |
          rm -rf dist
          mkdir -p dist
          cp index.html dist/
          cp -R assets dist/
          touch dist/.nojekyll
          for dir in q c; do
            if [ -d "$dir" ]; then
              cp -R "$dir" dist/
            fi
          done
          for file in CNAME robots.txt site.webmanifest sitemap.xml 404.html study.html mock.html googled42fe79d25dbdc9a.html; do
            if [ -f "$file" ]; then
              cp "$file" dist/
            fi
          done

      - name: Setup Pages
        uses: actions/configure-pages@v6

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: './dist'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
